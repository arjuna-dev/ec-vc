## 1) Description
In the shared create record dialog opened from `Test Shell`, the `Status` and `Priority` dropdown fields open without listing any options.

## 2) Environment
- Git branch: `ECS_Ingestion_Sandbox`
- OS: Windows
- App: Electron Quasar desktop app
- Runtime: Electron

## 3) Expected Behavior
When a canonical `Status` or `Priority` field renders as a dropdown in the shared create dialog, it should list the available options.

## 4) Actual Behavior
The dropdown opens, but no options are listed for `Status` and `Priority`.

## 5) Screenshots
