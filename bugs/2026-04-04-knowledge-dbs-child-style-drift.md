## 1) Description
The `Industries` and `Securities` child rows under `Knowledge DBs` do not match the visual style of the normal child rows under `Files` in the left main menu.

## 2) Environment
- Git branch: `ECS_Ingestion_Sandbox`
- Operating system: Unknown
- Runtime: Electron / Quasar desktop app
- App version: `MTK v0.0.1`

## 3) Expected Behavior
`Industries` and `Securities` should inherit the same child-row styling path as the regular file children under `Files`.

## 4) Actual Behavior
The `Knowledge DBs` child rows still look different from the normal file child rows because they carry custom child-row presentation.

## 5) Screenshots
