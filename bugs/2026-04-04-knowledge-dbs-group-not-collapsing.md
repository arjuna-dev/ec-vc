## 1) Description
The `Knowledge DBs` group in the left navigation is not behaving like a real collapsible branch. Its child items appear outside the grouping instead of only showing when the group is expanded.

## 2) Environment
- Git branch: `ECS_Ingestion_Sandbox`
- Operating system: Windows
- App/runtime: Electron Quasar desktop app
- App version: Unknown

## 3) Expected Behavior
`Knowledge DBs` should expand and collapse like a true nav branch, and its child files should only appear when that group is open.

## 4) Actual Behavior
The child items still appear outside the grouping behavior, so the `Knowledge DBs` branch does not feel properly nested or collapsible.

## 5) Screenshots
