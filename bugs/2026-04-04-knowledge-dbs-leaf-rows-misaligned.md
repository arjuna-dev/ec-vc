## 1) Description
The `Industries` and `Securities` rows under `Knowledge DBs` in the left main menu are not aligned like the normal file subset rows under `Files`.

## 2) Environment
- Git branch: `ECS_Ingestion_Sandbox`
- Operating system: Unknown
- Runtime: Electron / Quasar desktop app
- App version: `MTK v0.0.1`

## 3) Expected Behavior
`Industries` and `Securities` should use the same icon and text alignment pattern as the other secondary file rows in the left main menu.

## 4) Actual Behavior
`Industries` and `Securities` use a custom row layout that causes their icon and text alignment to drift from the normal subset rows under `Files`.

## 5) Screenshots
