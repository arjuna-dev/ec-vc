{
  "version": 1,
  "status": "active",
  "purpose": "Minimal workbook companion reference for the current file-owned runtime model.",
  "notes": [
    "This file is intentionally minimal and should not be treated as a live payload source.",
    "System Files owns the complete file structure and token registry at runtime.",
    "If workbook export/import is reintroduced, define a dedicated schema in /docs/000/Active before wiring runtime."
  ],
  "export_contract": {
    "format": "csv",
    "mode": "manual",
    "status": "not active"
  }
}
